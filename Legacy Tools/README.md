# Legacy Tools Archive

This folder holds old Classic Tilestorm conversion and parser tools that depend on the legacy map/database data format.

They are kept here as reference material only and are intentionally outside `Assets/` so Unity does not import them into the main project.

Current live tooling that remains in `Assets/` should be used for the model / geometry pipeline and other non-legacy manipulators.
