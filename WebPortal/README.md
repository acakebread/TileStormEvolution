# TileStorm Shared Maps

This folder is the public web portal for shared maps.

It provides:

- a lightweight landing page
- a manifest-driven map list
- separate thumbnails in `thumbs/<mapHash>.png`
- a short authoring guide for creators

Open `index.html` for the portal landing page.
